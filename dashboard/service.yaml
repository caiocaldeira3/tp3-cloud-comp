apiVersion: v1
kind: Service
metadata:
  name: dashboard-service
  namespace: caiocaldeira
spec:
  selector:
    app: dashboard-app
  ports:
  - protocol: TCP
    port: 52007
    nodePort: 52007
    targetPort: 8050
  type: NodePort